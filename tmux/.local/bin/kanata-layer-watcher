#!/usr/bin/env bash

KANATA_SERVER_PORT=${KANATA_SERVER_PORT:-4444}

data=$(echo | socat - TCP4:127.0.0.1:$KANATA_SERVER_PORT 2>/dev/null)

if [ -z "$data"]; then
	echo "󰌏  N/A"
else
	echo "󰌏  $(echo $data | jq ".LayerChange .new" | tr -d "\"")"
fi
